require 'spec_helper'

describe GroSocial do
  it 'has a version number' do
    expect(GroSocial::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
